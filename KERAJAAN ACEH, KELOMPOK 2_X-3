import React from "react";
import { motion } from "framer-motion";
import { ArrowLeft, ArrowUpRight, BookOpen, PenLine } from "lucide-react";
import {
    PintuAcehOrnament,
    DividerStar,
} from "./AcehOrnaments";

// --- KOMBINASI URL TERBARU ---
const MATERI_URL = "https://drive.google.com/drive/folders/1LNZMfm9C__ji5C_4uomqsddOELTd03Xp";
const LATIHAN_URL = "https://quiz.zep.us/en/play/lpn5OR"; // Sudah diupdate ke ZEP Quiz

const containerVariants = {
    hidden: { opacity: 0 },
    show: {
        opacity: 1,
        transition: { staggerChildren: 0.12, delayChildren: 0.05 },
    },
    exit: { opacity: 0, transition: { duration: 0.3 } },
};

const itemVariants = {
    hidden: { opacity: 0, y: 30 },
    show: {
        opacity: 1,
        y: 0,
        transition: { duration: 0.75, ease: [0.16, 1, 0.3, 1] },
    },
};

const MenuCard = ({
    href,
    icon: Icon,
    overline,
    title,
    desc,
    testId,
}) => (
    <motion.a
        variants={itemVariants}
        href={href}
        target="_blank"
        rel="noopener noreferrer"
        data-testid={testId}
        className="group relative block p-8 md:p-10 bg-black/60 backdrop-blur-xl border border-[rgba(212,175,55,0.2)] transition-all duration-500 hover:border-[rgba(212,175,55,0.6)] hover:bg-black/75 hover:-translate-y-1 hover:shadow-[0_20px_60px_-10px_rgba(212,175,55,0.25)]"
    >
        <span className="corner-frame tl" />
        <span className="corner-frame tr" />
        <span className="corner-frame bl" />
        <span className="corner-frame br" />

        <div className="flex items-start justify-between mb-8">
            <div className="p-4 border border-[rgba(212,175,55,0.35)] text-[color:var(--aceh-gold)] group-hover:bg-[rgba(212,175,55,0.1)] transition-colors duration-500">
                <Icon size={28} strokeWidth={1.4} />
            </div>
            <ArrowUpRight
                size={22}
                className="text-[color:var(--aceh-gold)]/60 group-hover:text-[color:var(--aceh-gold)] transition-all duration-500 group-hover:rotate-12 group-hover:scale-110"
            />
        </div>

        <p className="font-accent tracking-[0.35em] uppercase text-xs text-[color:var(--aceh-gold)] mb-3">
            {overline}
        </p>
        <h3 className="font-heading text-3xl md:text-4xl text-white mb-4 leading-tight">
            {title}
        </h3>
        <p className="font-body text-white/65 leading-relaxed text-sm md:text-base">
            {desc}
        </p>

        <div className="mt-8 pt-6 border-t border-[rgba(212,175,55,0.15)] flex items-center justify-between">
            <span className="font-accent italic text-sm text-[color:var(--aceh-gold)]/80 group-hover:text-[color:var(--aceh-gold)] transition-colors">
                Buka di tab baru
            </span>
            <span className="h-px w-12 bg-[color:var(--aceh-gold)]/40 group-hover:w-20 transition-all duration-500" />
        </div>
    </motion.a>
);

export default function MenuPage({ onKembali }) {
    return (
        <motion.section
            key="menu"
            data-testid="menu-page"
            variants={containerVariants}
            initial="hidden"
            animate="show"
            exit="exit"
            className="relative min-h-screen w-full overflow-hidden"
        >
            <div className="absolute inset-0 bg-[color:var(--aceh-bg)]" />
            <div className="absolute inset-0 aceh-pattern opacity-30" />
            <div
                className="absolute inset-0 opacity-[0.06] bg-center bg-cover"
                style={{
                    backgroundImage:
                        "url('https://images.pexels.com/photos/10559197/pexels-photo-10559197.jpeg')",
                }}
            />
            <div className="absolute inset-0 vignette" />
            <div className="absolute inset-0 grain" />

            {/* Top bar with back button */}
            <div className="relative z-20 px-6 md:px-12 py-6 flex items-center justify-between">
                <button
                    onClick={onKembali}
                    data-testid="kembali-button"
                    className="group inline-flex items-center gap-3 px-5 py-2.5 border border-[rgba(212,175,55,0.4)] text-[color:var(--aceh-gold)] font-accent tracking-[0.25em] uppercase text-xs md:text-sm hover:bg-[rgba(212,175,55,0.1)] hover:border-[color:var(--aceh-gold)] transition-all duration-300"
                >
                    <ArrowLeft
                        size={16}
                        className="transition-transform duration-300 group-hover:-translate-x-1"
                    />
                    Kembali
                </button>
                <div className="flex items-center gap-3 text-[color:var(--aceh-gold)]">
                    <span className="hidden md:inline font-accent text-xs tracking-[0.35em] uppercase opacity-70">
                        Portal Sejarah
                    </span>
                    <PintuAcehOrnament size={26} className="opacity-90" />
                </div>
            </div>

            <div className="relative z-10 max-w-7xl mx-auto px-6 md:px-12 py-8 md:py-16">
                <div className="grid grid-cols-1 md:grid-cols-12 gap-10 md:gap-16 items-start">
                    {/* Left: Title */}
                    <div className="md:col-span-5 md:sticky md:top-20">
                        <motion.div
                            variants={itemVariants}
                            className="flex items-center gap-3 mb-6 text-[color:var(--aceh-gold)]"
                        >
                            <span className="h-px w-10 bg-[color:var(--aceh-gold)]" />
                            <span className="font-accent italic text-sm tracking-[0.3em] uppercase">
                                Bab II
                            </span>
                        </motion.div>

                        <motion.h1
                            variants={itemVariants}
                            className="font-heading text-5xl md:text-6xl lg:text-7xl font-bold leading-[1.02] mb-6 text-gold-gradient"
                            data-testid="menu-title"
                        >
                            Eksplorasi
                            <br />
                            <span className="italic font-medium">Aceh</span>
                        </motion.h1>

                        <motion.div
                            variants={itemVariants}
                            className="gold-divider mb-8 justify-start"
                        >
                            <DividerStar />
                        </motion.div>

                        <motion.p
                            variants={itemVariants}
                            className="font-body text-white/70 text-base md:text-lg leading-relaxed mb-8 max-w-md"
                        >
                            Silakan pilih langkah Anda selanjutnya — pelajari
                            naskah sejarah kesultanan yang pernah menguasai
                            Selat Malaka, atau uji pemahaman Anda lewat latihan
                            soal.
                        </motion.p>

                        <motion.p
                            variants={itemVariants}
                            className="font-accent italic text-[color:var(--aceh-gold)]/80 text-base md:text-lg max-w-md"
                        >
                            “Ilmu adalah cahaya, dan Aceh adalah Serambi
                            Mekkah — tempat cahaya itu pernah paling terang.”
                        </motion.p>
                    </div>

                    {/* Right: Cards */}
                    <div className="md:col-span-7 flex flex-col gap-6 md:gap-8">
                        <MenuCard
                            href={MATERI_URL}
                            icon={BookOpen}
                            overline="Arsip Kerajaan"
                            title="Pelajari Materi"
                            desc="Koleksi naskah, peta, dan dokumen sejarah Kerajaan Aceh Darussalam. Pelajari masa kejayaan Sultan Iskandar Muda, ulama besar seperti Syiah Kuala, hingga perlawanan terhadap kolonial."
                            testId="materi-link"
                        />
                        <MenuCard
                            href={LATIHAN_URL}
                            icon={PenLine}
                            overline="Uji Pemahaman"
                            title="Latihan Soal"
                            desc="Asah pemahaman Anda melalui kumpulan soal interaktif di platform ZEP Quiz. Diskusikan peristiwa penting dan warisan budaya Kesultanan Aceh Darussalam secara seru."
                            testId="latihan-link"
                        />
                    </div>
                </div>

                <motion.div
                    variants={itemVariants}
                    className="mt-20 md:mt-28 text-center font-accent italic text-sm text-[color:var(--aceh-gold)]/60 tracking-widest"
                >
                    ·   ·   ·
                    <p className="mt-3 not-italic tracking-[0.4em] uppercase text-xs">
                        Darussalam · Negeri Yang Damai
                    </p>
                </motion.div>
            </div>
        </motion.section>
    );
}
